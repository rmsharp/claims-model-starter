# Backlog

**Open work only.** Completed items move to `CHANGELOG.md` (chronological, session-numbered). Milestone-grouped summaries live in `ROADMAP.md`. **Do not leave checked-off `[x]` items here** — remove the line on completion and record the work in `CHANGELOG.md` per `docs/methodology/README.md` §templates (v2.1 three-file split).

## Open Items

- [ ] **Classify transport timeouts as transient in the interview-convergence sweep** (surfaced Session 170; ready-for-agent). The gap #1c sweep retries/excludes only `IntakeLLMError`/`StakeholderSimError` (`tests/eval/interview_sweep.py::_TRANSIENT_ERRORS`). A network `anthropic.APITimeoutError` (and `anthropic.APIConnectionError`) propagates **raw** — `_call_json` does not wrap it (`src/model_project_constructor/agents/intake/anthropic_client.py:286`) — so it aborts `test_live_interview_converges` instead of being retried/excluded (observed live S170: 1 of 3 gate runs aborted on an SSL handshake timeout; a clean re-run then passed). The convergence *rate* is unaffected, but the gate stays fragile to a network blip. **Acceptance criteria:** (1) a transport timeout/connection error during a live interview is classified transient (retried up to the existing bound, then excluded with a note) — pick **either** adding `anthropic.APITimeoutError`/`anthropic.APIConnectionError` to `_TRANSIENT_ERRORS`, **or** wrapping them in `IntakeLLMError` at the `_call_json` seam (the latter also helps the data agent); (2) a new deterministic test in `tests/eval/test_interview_sweep.py` (injected `run_one` raising the transport error) proves retry-then-exclude, **no live run needed**; (3) genuine non-convergence and real harness bugs still behave as before (FM #18 — don't broaden the catch into swallowing real bugs); (4) `_MAX_TRANSIENT_RETRIES`/threshold unchanged (#129). Note the design choice (tuple vs seam-wrap) in the commit.
- [ ] **Quarto explainer deck — "How the live interview-convergence test works"** (requested by operator, Session 170-06-18; ready-for-agent). Produce a Quarto (`.qmd`) **presentation** explaining the live interview-convergence measurement (§3.4 / gap #1c machinery) for a **technical audience with introductory LLM (large language model) knowledge**. **Source material:** the Session 170 walkthrough in SESSION_NOTES.md (the two-LLM interview loop + stakeholder simulator, the `interview_converged` scorer, the N≥5 sweep + transient-retry/exclude robustness layer, the 4-converging + 1-negative-control fixture corpus, the gate-vs-`shadow_run` call sites). **Cite these files:** `tests/eval/test_eval_live.py`, `tests/eval/interview_sweep.py`, `tests/eval/eval_scoring.py` (`interview_converged`), `tests/eval/eval_corpus.py` (`_INTERVIEW_SOURCES`), `tests/eval/shadow_run.py`, `tests/eval/eval_thresholds.py`. **Acceptance criteria:** (1) a `.qmd` that renders cleanly (`quarto render`) to a slide deck (revealjs default, beamer acceptable); (2) every initialism expanded on first use (LLM, P&C, API, QC, SME, §3.4); (3) sections cover — the capability & why it matters in the 6-step pipeline · how one sample runs (the two-LLM loop) · the precise convergence definition (`questions_cap_reached` marker) · the N≥5/95% statistics layer and the gap #1c robustness rationale (why 4-once → 4/4 was fragile) · the corpus incl. the negative-control cap fixture · gate vs producer; (4) output to `docs/presentations/` (create the dir). Confirm format (revealjs vs beamer) with the operator if unsure.
