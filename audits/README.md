# Audits

Formerly `docs/audits/` — moved out of `docs_dir` in Phase A1 of
`docs/planning/enterprise-migration.md` (D7) so `mkdocs.yml`'s publication
scope can no longer include this directory, structurally rather than via a
config a future edit could undo.
